---
title: A modern full-stack programmer's journey and ideas.
layout: blog
---

body
  div#title
    div
      a href="/blog" moodmosaic
      span &nbsp;-&nbsp;
      a href="/" A modern full-stack programmer's journey and ideas.
    div.line
  div#main
    div#postlist
      - for item in catalog do
        div.post data-tags="#{item.categories.map!{|x| x.downcase.gsub(' ','-')}.join(",")}"
          h2
            a href=item.url
              = item.title
          div.undertitle
            span.meta 
              = item.slug
          div.undertitle
            span.date #{item.day}/#{item.month}/#{item.year} 
            - for category in item.categories
              span.tag onclick="filterTag(&quot;#{category.downcase.gsub(' ','-')}&quot;)" #{category.downcase.gsub(' ','-')}

  == javascript_include_tag 'filter'
